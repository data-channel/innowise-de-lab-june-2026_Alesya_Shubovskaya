# 1. Входные данные
products = ["Яблоки", "Хлеб", "Молоко", "Печенье", "Сок", "Кефир"]

# 2. Проходим по каждому второму индексу (0, 2, 4, ...)
for i in range(0, len(products), 2):
    # 3. Получаем название товара по индексу i
    product_name = products[i]

    # 4. Проверяем, есть ли в списке "Бананы"
    if "Бананы" in products:
        print("Обнаружены бананы! Проверка прервана.")
        break  # 5. Прерываем цикл

    # 6. Выводим результат для текущего товара
    print(f"Индекс {i}: Проверен товар «{product_name}» (Длина названия: {len(product_name)} символов)")

# 7. Блок else — выполняется, если цикл не был прерван break
else:
    print("--- Выборочная проверка успешно завершена ---")